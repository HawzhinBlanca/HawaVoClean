"""HawaVoClean engine bridge: a loopback-only HTTP API over the pipeline.

``hawavoclean serve`` starts it; the UI bundle (browser, Electron, DaVinci
Resolve) talks to it. Binding contract: ``docs/ui-contract.md``.
"""
